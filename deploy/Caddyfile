# Caddy gets and renews HTTPS certificates automatically.
# DOMAIN and ACME_EMAIL come from the .env file (see docker-compose.yml).
{
	email {$ACME_EMAIL}
}

www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}

{$DOMAIN}, admin.{$DOMAIN} {
	encode zstd gzip
	reverse_proxy app:3000
}
